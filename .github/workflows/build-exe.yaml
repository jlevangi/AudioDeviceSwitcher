name: Build EXE

on:
  push:
    branches: [master]
    tags: ['v*']
  pull_request:
    branches: [master]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest
    strategy:
      fail-fast: false
      matrix:
        platform: [x64, arm64]

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup MSBuild
        uses: microsoft/setup-msbuild@v2

      - name: Setup .NET SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Set artifact version
        shell: pwsh
        run: |
          $version = if ($env:GITHUB_REF_TYPE -eq 'tag') {
            $env:GITHUB_REF_NAME.TrimStart('v')
          } else {
            "1.1.0-ci.$env:GITHUB_RUN_NUMBER"
          }
          "ARTIFACT_VERSION=$version" >> $env:GITHUB_ENV

      - name: Restore
        run: msbuild -v:m -nologo -p:Configuration=Release -p:Platform=${{ matrix.platform }} -t:restore

      - name: Publish
        run: >
          msbuild -v:m -nologo
          -p:Configuration=Release
          -p:Platform=${{ matrix.platform }}
          -p:PublishSingleFile=true
          -p:SelfContained=true
          -p:WindowsAppSDKSelfContained=true
          -p:RuntimeIdentifier=win10-${{ matrix.platform }}
          -p:PublishTrimmed=false
          -p:AppxPackageSigningEnabled=false
          -t:Publish
          src/AudioDeviceSwitcher/AudioDeviceSwitcher.csproj

      - name: Package EXE
        shell: pwsh
        run: |
          $publishDir = Get-ChildItem "src/AudioDeviceSwitcher/bin" -Directory -Recurse |
            Where-Object Name -eq publish |
            Where-Object FullName -match "win10-${{ matrix.platform }}" |
            Select-Object -First 1
          if (-not $publishDir) { throw "Publish directory not found" }
          New-Item dist -ItemType Directory -Force | Out-Null
          $zip = "dist/AudioDeviceSwitcher-${{ matrix.platform }}-v$env:ARTIFACT_VERSION.zip"
          Compress-Archive -Path "$($publishDir.FullName)/*" -DestinationPath $zip

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: AudioDeviceSwitcher-${{ matrix.platform }}
          path: dist/*.zip

      - name: Upload release asset
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: dist/*.zip
          draft: true
